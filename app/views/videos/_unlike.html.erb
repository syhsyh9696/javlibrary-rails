<div id="like_form">
<%= form_for current_user, :url => unfave_videos_path, :html => {:method => :delete}, :remote => true do |f| %>
  <div><%= hidden_field_tag :video_id, @video.id %></div>
  <%= f.submit "Unlike", class: "btn btn-secondary btn-lg" %>
<% end %>
</div>
