<%= form_for current_user, :url => url_for(:controller => 'relationships', :action => 'destroy'), :html => {:method => :delete} do |f| %>
  <div><%= hidden_field_tag :actor_id, @actor.id %></div>
  <%= f.submit "Unfollow", class: "btn btn-primary" %>
<% end %>
